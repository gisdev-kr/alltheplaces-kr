import type { PoiFeature } from "../data/types";
const text=(value:unknown)=>value==null?"":String(value);
export function DetailPanel({feature,onClose}:{feature:PoiFeature;onClose:()=>void}){const p=feature.properties||{};const title=text(p.name||p.branch||p.brand||p.ref||"이름 없는 POI");const source=text(p["@source_uri"]||p.website);const wikidata=text(p["brand:wikidata"]);return <aside className="detail-panel"><button className="close" onClick={onClose} aria-label="닫기">×</button><span className="detail-kicker">{text(p.brand||p["@spider"]||"POI")}</span><h2>{title}</h2><dl><div><dt>주소</dt><dd>{text(p["addr:full"])||"—"}</dd></div><div><dt>전화</dt><dd>{text(p.phone)||"—"}</dd></div><div><dt>Spider</dt><dd>{text(p["@spider"])||"—"}</dd></div><div><dt>Ref</dt><dd>{text(p.ref)||"—"}</dd></div><div><dt>OSM 검토</dt><dd><span className="status">{text(p.match_status)||"needs_review"}</span></dd></div><div><dt>Wikidata</dt><dd>{wikidata?<a href={`https://www.wikidata.org/wiki/${wikidata}`} target="_blank" rel="noreferrer">{wikidata} ↗</a>:"—"}</dd></div><div><dt>NSI</dt><dd>{text(p.nsi_id)||"—"}</dd></div></dl>{source&&<a className="source" href={source} target="_blank" rel="noreferrer">원본 페이지 열기 ↗</a>}</aside>}

